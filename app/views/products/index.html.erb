<h2> Products: </h2>

<% @products.each do |product| %>
  <h4> Name: <%= product.name %> </h4>
  <h6><%= product.description %></h6>
  <h6><%= link_to("Buy #{product.name} here!", product.url) %></h6>
   <img src=<%="#{product.image_url}" %>>
  <hr>
<% end %>